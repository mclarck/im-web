import GqlProvider, {Apollo} from "./GqlProvider";
import Rest from "./rest";
import RestProvider, {RestClient} from "./RestProvider";
import Store from "./storage";

export {
    RestClient,
    Rest,
    Apollo,
    Store,
    GqlProvider,
    RestProvider,
};
